{
  "name": "stac-network-proto",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "live-server --port=5500",
    "build": "echo 'no build step configured'"
  },
  "dependencies": {},
  "devDependencies": {
    "live-server": "^1.2.1"
  }
}
